# Case sensitive matches only
CASE_SENSITIVE="true"

# Source and load all things used manually,
# I prefer the explicitness here over a loop

source ~/.dotfiles/system/init.zsh
source ~/.dotfiles/apps/starship/init.zsh