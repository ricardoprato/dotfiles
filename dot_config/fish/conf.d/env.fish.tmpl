# Auto-generated by chezmoi from manifest.toml [env] block (D-29 target 1).
# Do not edit — edit ~/.config/dotfiles/manifest.toml instead.
# Reload: open a new fish shell after chezmoi apply.
#
# Note: this file loads AFTER dot_config/fish/conf.d/enviroment.fish (alphabetical).
# set -gx here overrides the EDITOR set via `which nvim` in enviroment.fish
# (set -gx in later conf.d file wins for the global scope).
{{- range $k, $v := .env }}
{{-   if not (eq $k "path_prepend") }}
set -gx {{ $k }} {{ $v | quote }}
{{-   end }}
{{- end }}
{{- with $pp := index .env "path_prepend" }}
{{-   range $pp.entries }}
fish_add_path -p {{ . | quote }}
{{-   end }}
{{- end }}
