# Makes hyprsunset do nothing to the screen by default
# Without this, the default applies some tint to the monitor
profile {
    time = 07:00
    identity = true
}

# Auto-switch to nightlight at 20:00 (requires hyprsunset launched on session start).
profile {
    time = 20:00
    temperature = 4000
}
